import ${Extends} = require('replace/path/${Extends}');

/**
 * TODO: YUIDoc_comment
 *
 * @class ${NAME}
 * @extends ${Extends}
 * @constructor
 **/
class ${NAME} extends ${Extends} {

    constructor() {
        super();
    }

}

export = ${NAME};