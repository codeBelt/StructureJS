import BaseEvent from 'structurejs/event/BaseEvent';

/**
 * TODO: YUIDoc_comment
 *
 * @class ${NAME}
 * @extends BaseEvent
 * @constructor
 **/
class ${NAME} extends BaseEvent {

     /**
     * This is an example of an event type.
     * Go ahead and rename or remove this.
     *
     * @event EXAMPLE
     * @type {string}
     * @static
     */
    static EXAMPLE = '${NAME}.example';
        
    constructor(type, bubbles, cancelable, data) {
        super(type, bubbles, cancelable, data);
    }
    
}

export default ${NAME};
